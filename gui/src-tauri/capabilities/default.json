{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core APIs plus permission to run the bundled `helix` sidecar for local node/validator management.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/helix",
          "sidecar": true
        }
      ]
    }
  ]
}
