/target/
/builds/
**/*.rs.bk
.env
*.log
# Validator secret key — never commit
/validator-key.json
/validator-wallet.json
/faucet-key.json
# Double-sign high-water mark — per-node runtime state, next to the key (see signing_guard).
# Glob covers the live file, its atomic .tmp, and any .pre-<reason>-reset.<ts>.bak snapshots.
*.signing-state.json*
# Chain-Datenbank — Laufzeitdaten, kein Quellcode
/helix-data.redb
/helix-data.redb.*.bak
# Scripts (Cron/Dev-Loop — nur lokal)
/scripts/
/CLAUDE.md
/CLAUDE-archiv.md
/.claude-session-state*.md
